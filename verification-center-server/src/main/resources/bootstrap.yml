spring:
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
  application:
    name: authenticationservice
logging:
  level:
    root: info
    com.eyescloud: debug